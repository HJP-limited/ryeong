# HJP — AI 명함 관리 앱

명함 사진 → 온디바이스 OCR → DB 저장 → Single ReAct Agent(온디바이스 LLM)가
자연어로 명함 검색·메일 초안·일정 등록 등을 수행하는 Android 앱.
팀 프로젝트이며 팀원 전원이 앱 개발 초심자. 이 세션 사용자의 담당은
`create_calendar_event`, `open_compose` 도구 (구현 완료).

## 빌드

PowerShell에서 `JAVA_HOME`이 비어 있으므로 Android Studio JBR을 지정해야 함:

```powershell
$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"; .\gradlew.bat :app:assembleDebug --console=plain
```

- Kotlin 2.2 / AGP 9.2 / compileSdk 36 / **minSdk 24** / Jetpack Compose (Material3)
- minSdk 24 제약: `java.time` 사용 불가 (desugaring 미설정) → `java.text.SimpleDateFormat` 사용
- IDE 진단이 새 파일을 못 잡아 가짜 Unresolved reference를 내는 경우가 있음 — 실제 빌드로 검증할 것

## 기술 스택 (팀 결정사항)

- 에이전트: Single ReAct Agent, 도구 9개 예정
- 온디바이스 LLM: Gemma 4 E2B (LiteRT-LM, `LlmRole.Chat` 1순위 — 2026-07-23 연동, 실기기 검증 전), 임베딩: EmbeddingGemma 300M
- 검색: 키워드(Room FTS4) + 시맨틱(EmbeddingGemma) + RRF(k=60) + 라우팅/필드필터/기권

## 검색·조회 (이 세션 사용자 담당)

**Kotlin 이 정본이다.** `scripts/eval_search.py` / `scripts/hybrid_server.py` 는 노트북에서
지표를 재고 눈으로 확인하는 용도이고, 실제 동작은 아래 Kotlin 코드다. 규칙을 바꾸면 양쪽을
같이 고치고 `:app:testDebugUnitTest` 로 두 구현이 같은 판정을 내는지 확인할 것.

파이프라인: `QueryAnalyzer(KeywordSearchRanker) → keyword(FTS4 티어드) → semantic →
RRF(k=60) → 필드 조건(필터/정렬) → 기권 게이트 → RagContext → Gemma → LLM 무관 판정 반영`

- `search/CardGazetteer.kt` — 카드 데이터에서 실제 존재하는 값 집합(지역/이름/직함/회사/부서)을
  뽑아 두고 그걸로 판정한다. 점수 컷오프 방식은 개념형 질의를 같이 죽여서 폐기했다
  (임계값 스윕에서 양립 지점이 없었음: 0.04 → 74%/44%, 0.20 → 0%/100%).
  - 기권: 없는 지역("세종특별자치시", "울릉도") / 없는 이름("정하은씨", 호칭 없는 "정하은") /
    매칭 없는 식별자
  - **기권은 '특정 값을 지목했는데 그 값이 데이터에 없다'일 때만 건다.** 점수·매칭수 같은
    신호로는 못 건다 — 실측(29질의)에서 top1·top10평균·top50평균·격차·대비 5종이 전부
    겹쳤다("백령도"(정답0) 0.423 > "공무원 직급"(정답31) 0.280). "키워드 0건이면 기권"도
    안 된다: 기권은 19/20 로 오르지만 개념형 10개가 죽는다(정답 7~147명짜리들).
    키워드 0건은 곧 '시맨틱만이 답할 수 있는 영역'이라, 그걸 자르면 시맨틱을 넣은 이유가 사라진다.
  - 필드 조건 추출: 이름 → **직함** → 지역 순으로 본다. 직함이 지역보다 먼저인 게 중요하다 —
    "상무"는 직함이면서 광주 '상무대로'의 어간이기도 한데, 지역으로 새면 주소에 상무가 있는
    14명만 남고 진짜 상무 92명이 전멸한다. 예전에는 이 충돌을 어휘 차감
    (`address_terms -= title_terms`)으로 회피했지만, 그러면 "상무 찾아줘"에 아무 조건도
    안 걸렸다. 지금은 **판정 순서**로 해결하므로 그 규칙은 없다.
  - 이름·지역 조건 → **하드 필터**(그 조건을 만족하는 카드만 남김. 이름 질의 P@5 0.455 → 1.000)
  - 직함 조건 → 상황에 따라 다르다:
    - 지역/이름과 **함께** 오면 하드 필터(지역+직함 P@5 0.395 → 0.815).
      **조건을 다 만족하는 사람이 없으면 빈 결과 = 기권이다.** 완화하지 않는다.
    - 직함**만** 오면 지우지 않고 **정확 일치자를 앞으로 정렬**한다. 지우면 시맨틱이 찾아낸
      유사 직함('고문변호사')이 사라지고, 안 건드리면 RRF가 그걸 정확 일치자 위로 올린다
      (실측 92질의: 1위 정확일치 92/92 → 86/92로 하락). 정렬은 양쪽을 다 잡는다.
  - **직함 완화 규칙은 삭제했다**(예전에는 조합 결과가 0명이면 직함 조건만 떼고 다시 찾았다).
    측정해 보니 **eval 203질의에서 한 번도 발동하지 않았다** — 질의가 데이터에 실제로 있는
    조합으로 만들어지기 때문이다. 즉 어떤 지표도 떠받치지 않으면서, 사용자가 **없는 조합**을
    물을 때만 발동해 거짓 답을 만들었다("판교에 있는 디자이너" → 판교에 디자인 계열 0명인데
    직함을 떼고 판교 9명을 넘겨서 LLM 이 주임 한 명을 디자이너라고 답함).
    제거 후 지표는 소수점까지 동일했고 그 질의는 기권으로 바뀌었다.
  - 직함 매칭은 **단어 단위**다(부분 문자열이면 "대표이사"가 "이사"에 걸린다). 그래서 띄어쓰기
    없는 합성 직함('고문변호사')은 정확 일치로 안 잡히는데, 부분 문자열/접미사 완화를 둘 다
    측정했더니 순정보다 나빴다(P@5 0.773 → 0.761 / 0.769). 데이터 표기 정규화가 맞는 해법.
  - 라우팅: 전화/이메일 질의는 시맨틱을 융합에서 뺀다(P@5 0.233 → 1.000)
  - **후보가 0장이면 LLM 을 부르지 않는다** — 기권과 같은 처리다. 빈 컨텍스트를 넣었더니
    2B 모델이 컨텍스트 문자열("검색 후보 없음")을 그대로 답변으로 뱉었다(pass^3 3회 재현).
  - **RAG 컨텍스트 헤더에 후보 개수를 쓰지 않는다.** 예전엔 "검색 후보: 총 N명"을 맨 앞에
    넣었는데(모델이 개수를 세다 틀리는 걸 막으려고), 정작 모델이 **그 숫자를 답으로 그대로
    옮겨 적었다**: "디자인하는 사람 찾아줘" -> 이름 대신 "총 3명". 후보 개수는 '보여준 카드
    수'일 뿐 답이 아니다. 셀 수 있는 조건일 때만 `countByCondition` 의 **진짜 전체 수**를 넣는다.
  - 부서(department) 어휘는 **카운트 경로 전용**이다. 공용 필터에 넣어봤다가 뺐다 —
    204질의에서 발동 0건이라 회귀 검증 자체가 불가능했고("안전"이 아니라 "미측정"),
    목표였던 "AI 개발자 몇 명?"도 못 고쳤다(부서명이 "AI개발팀" 붙여쓰기라 정확 일치 실패).
- 조건 카운트(`countByKnownCondition`) — "판교에 몇 명이야?", "이사 직급 몇 명?" 같은 질의는
  검색(top-5 / FUSION_POOL 컷)을 안 태우고 전체 카드를 직접 센다. 검색에 태우면 실제 92명인데
  "총 5명"으로 답한다. 가제티어가 모르는 개념형 조건이면 null → 기존 검색+LLM 경로로 폴백.
  부서는 여기서만 부분 문자열로 맞춘다("AI 개발자 몇 명?" → 42명).
- **답변 후처리(`narrowByAnswer`)** — LLM 답변을 보고 화면에 띄울 카드를 정한다.
  검색은 top-5 를 채우느라 무관한 후보를 함께 담으므로 그대로 두면 답변과 카드가 어긋난다.
  순서대로 판정한다:
  1. 거절 문구(`REJECTION_MARKERS`)가 있으면 → 카드 전부 비움
  2. `총 0명` → 숫자로 표현된 거절이므로 카드 비움
     (실측: "울릉도 근무자" → '총 0명'인데 카드 5장이 뜨던 모순)
  3. 답변이 어떤 카드를 **근거로 삼았으면** 그 카드만 남김. 근거 판정은 이름뿐 아니라
     **회사·주소·이메일·전화 뒷자리**도 본다 — "문선영씨 회사가 어디야?" → "주식회사
     노블어패럴입니다." 처럼 **필드 값으로만 답하는 게 정상인 질문이 많다.** 이름만 보다가
     그런 답변의 카드를 통째로 지우는 회귀를 냈었다. 주소는 LLM 이 괄호를 생략하는 일이
     잦아 앞부분(6자 이상) 일치도 인정한다.
  4. **이름 조건으로 후보가 1명이면 그대로 남김.** 이 단계가 하는 일은 '후보 여럿 중 답변이
     가리키는 사람 고르기'인데 후보가 하나면 고를 게 없다. 실측 회귀(멀티턴): "직급은?" →
     "AI 개발자", "부서는?" → "데이터사이언스팀" 처럼 **필드 값만 짧게 답하면** 이름·회사·
     주소가 답변에 없어서 카드가 매 턴 사라졌다. 경계는 테스트로 고정했다 — 동명이인 2명이면
     예전대로 비우고, 이름 조건 없이 우연히 1명이 남은 경우도 비운다("오늘 날씨 어때?" 방어).
  5. `총 N명`인데 N < 후보 수 → 상위 N개만 남김. 정렬이 정확 일치를 앞에 두므로 상위 N개가
     그 N명이다(실측: "AI 개발하는 사람" → '총 2명'인데 카드 5장, 뒤 3장은 AI개발팀이지만
     직함이 대표이사·디자인 디렉터였다).
  6. 이름도 필드도 `총 N명`도 없으면 → 명함과 무관한 답변이므로 카드 비움
     (실측: "오늘 날씨 어때?" 에 무관한 5명이 그대로 뜨던 버그)
- `agent/AgentSession.kt` — 멀티턴. 최근 메시지 8개 window + 3,000자 rolling summary +
  tool_session_context. 요약은 **모델 호출 없이** 결정적으로 압축한다(온디바이스 지연 유지).
- `agent/ConversationalFollowup.kt` — "5명인데?", "그 사람들 …" 같이 검색할 내용이 없는
  발화는 재검색하지 않고 직전 턴 카드를 근거로 쓴다.
- **생략형 후속은 두 방향이고 둘 다 처리한다.** 한쪽만 있으면 절반은 조용히 깨진다.
  - 주어 생략 + 속성 명시 → `resolveSearchQuery` — "주소는?", "전화번호 뒤 4자리는 뭐야"
    처럼 속성 명사로 시작하면 직전 인물(focus)을 앞에 붙여 검색한다. 단, 질의에
    **새 검색값**이 있으면 붙이지 않는다(예: "번호 뒷자리 4312인 분"). 그 판정을 '숫자가
    하나라도 있으면'으로 했다가 버그가 났다 — "전화번호 뒤 **4**자리는 뭐야"의 4 가 새
    값으로 잡혀 직전 인물이 안 붙고 엉뚱한 검색이 됐다. 지금은 `\d{3,}`일 때만 새 값으로 본다.
  - 주어 명시 + 속성 생략 → `carryOverAttribute` — "음가영씨는?" 처럼 사람만 말하면 직전
    턴이 물어본 속성(`KEY_LAST_ATTRIBUTE`)을 이어 붙인다. 이게 없을 때 검색은 맞는데
    (그 사람 카드를 찾음) 답변이 "음가영"처럼 **이름만** 되돌아왔다(pass^3 3회 모두 재현).
    이미 속성 명사가 있거나 문장형이면 손대지 않는다.
- 상수 동기화: `RRF_K=60`, `FUSION_POOL=150` 은 `eval_search.py` 와 같은 값이어야 한다.
  `FUSION_POOL` 은 융합·필터용 후보 풀 크기일 뿐이고, LLM 에 넘기는 카드와 화면 표시는
  항상 top-5(`ragContext(5)`)라 풀을 키워도 온디바이스 생성 시간은 안 늘어난다.

## 멀티턴 지표 (`scripts/eval_multiturn.py`)

**검색 지표(`eval_search.py`)만으로는 대화가 이어지는지 알 수 없다.** 시나리오 139개 /
턴 386개를 카드 데이터에서 생성해 잰다. 서버를 먼저 띄워야 한다.

```
python scripts/eval_multiturn.py                                  # 1~3층, 386턴 ~40초
python scripts/eval_multiturn.py --generate                        # 4층 전량, ~65분
python scripts/eval_multiturn.py --generate --sample 18 --repeat 3  # 4~5층 + pass^3, ~50분
```

| 층 | 지표 | 생성 | 값 |
|---|---|---|---|
| 1 | **라우팅 정확도** — 의도한 경로로 갔나 | 불필요 | **1.000** (373/373) |
| 2 | **JGA** — 그 턴의 모든 슬롯(focus+필드조건)이 정답과 일치 | 불필요 | **1.000** (288/288) |
| 3 | **턴별 Hit@5** — 정답이 top-5 에 하나라도 있는가 | 불필요 | **0.994** (337/339) |
| 3' | **턴별 R@5 / MRR** — 상한 보정(분모 min(정답수,5)) | 불필요 | 아래 참조 |
| 4 | **체크리스트(LLM 턴만)** | 필요 | **0.990** (286/289) |
| 4' | **무관 요청 카드 억제** — 명함과 무관한 발화에 카드를 안 띄우나 | 필요 | **0.900** (9/10) |
| 5 | **pass^3** — 3번 다 통과한 시나리오(표본 18) | 필요 | **1.000** (18/18) |

**왜 이 조합인가**: 답변의 35%가 LLM을 안 탄다(카운트·기권·문맥참조·자기참조). 그런 턴은
답변이 고정 문자열이라 텍스트를 채점해도 라우팅만 맞으면 자동 통과다 — 전체를 뭉친
통과율 하나로 내면 그 턴들이 숫자를 채워 어려운 턴을 가린다. 그래서 **체크리스트는 LLM이
실제로 생성한 턴에만** 매기고, 주 지표는 라우팅·JGA다. 1~3층은 생성이 없어 40초라 매
변경마다 돌릴 수 있고, 4~5층은 느리지만 1~3층이 못 보는 답변 형식 문제를 잡는다.

**시나리오는 반드시 다중 턴이 주가 되게 할 것.** 처음엔 1턴이 57%였는데 그러면 이름만
멀티턴이고 실제로는 단발 검색을 재는 것이다. 지금은 2.7%(단발로만 확인되는 전체 카운트·
자기참조·전화 기권 3개뿐). 유형: 이름+생략형후속(4턴) / 6턴 장문 / 주제 전환 / 기권 후 복구 /
없는 조합 후 복구 / 카운트→좁히기 / 개념형→후속 / 동명이인 / 문맥참조 / 사실 정정 / 도구 범위 밖.

**6턴이 필요한 이유**: 최근 창이 4턴(`MAX_RECENT_MESSAGES=8`)이라 4턴짜리로는 롤오버가
한 번도 안 일어나 `historyDigest`가 검증되지 않는다.

**외부 대화 코퍼스(AI Hub 등)는 쓸 수 없다** — 거기엔 우리 카드가 없어서 "이 턴의 정답"을
정의할 수가 없다. 시나리오를 카드에서 생성하므로 데이터를 다시 만들면 같이 갱신된다.

**에이전트 도입 시**: 지금 `route`는 '우리 코드가 고른 경로'지만 도구가 여러 개가 되면
'모델이 고른 도구'로 의미가 바뀐다. 시나리오·정답은 도구 중립적으로 두었으므로 바뀌는 건
라벨 층 하나다. `도구 범위 밖` 유형("내일 3시에 일정 잡아줘")이 그 전환 지점이다 —
지금은 "명함을 들이밀지 않는다"를 재고, 캘린더 도구가 붙으면 "그 도구로 라우팅"이 된다.
JGA·턴별 R@5·체크리스트·pass^k는 그대로 유효하다.

**직전 결과를 가리키는 발화는 재검색이 아니다** — 세 가지를 같은 원리로 처리한다.
검색 지표는 이 셋 다 정상으로 보이므로(검색 자체는 멀쩡했다) 멀티턴 평가로만 잡힌다.
- **조건 누적** `applyNarrowing` — "대전에 있는 사람" → "그중에 변호사만" 에서 지역 조건이
  사라져 **전국 변호사 65명**이 나왔다. 조건을 따로 병합하지 않고 앞 턴 조건어를 질의 앞에
  붙인다(focus 를 앞에 붙이는 방식과 동일). 필터만 합치면 후보 풀에 대전 사람이 없어
  걸러낼 대상 자체가 없을 수 있는데, 질의에 넣으면 검색이 실제로 데려온다.
- **순서 지시** `ordinalIndex` — "두 번째 사람 연락처"가 새 검색으로 빠져 무관한 5명을
  데려왔다. 직전 집합의 인덱스로 읽어 후속 경로로 보낸다("마지막"은 -1).
- **복수 지시어** `isFollowup` — "그 사람들 …"은 직전 카드를 근거로 다시 답한다.

**이 평가가 실제로 잡아낸 버그 6개**(전부 검색 지표로는 안 보였다):
1. `context_answer`가 복수 지시어 후속을 삼킴 — "그 사람들 회사 알려줘"가 `CONTEXT_REFERENCES`의
   "그 사람"에 걸려 직전 답변을 재생했다(21건). context_answer를 followup **뒤로** 옮겼다.
   되짚는 발화("아까 뭐였지")만 context_answer로 가고, 직전 집합에 새로 묻는 발화는 다시 답한다.
2. 조사 `"에는"` 미처리 — "판교에"는 되는데 "그럼 판교에는"은 지역 조건이 안 걸렸다.
3. 후보 0장인데 LLM 호출 — 빈 컨텍스트를 넣으니 2B 모델이 컨텍스트 문자열("검색 후보 없음")을
   그대로 답변으로 뱉었다. 기권과 같이 처리한다(10초 → 0.2초).
4. 속성 생략 후속 미처리 — "음가영씨는?" 에 이름만 답했다(아래 생략형 후속 참조).
5. `context_answer`가 대명사 질문을 가로챔 — "그 사람 부서는?", "그 회사 주소는?" 가
   직전 답변 재생으로 샜다. `CONTEXT_REFERENCES` 에서 대명사를 뺐다(되짚기 표현만 남김) —
   대명사는 `FOLLOWUP_PRONOUNS` 의 focus 치환이 이미 담당하므로 중복이었다.
   **부수 효과로 "본인 회사 vs 명함 인물" 혼동도 함께 해결됐다** — 본인이 "내 회사는 …"
   이라고 말해 둔 뒤 "그 사람 회사 어디야"에 **본인** 회사를 답하던 문제가 같은 원인이었다.
6. focus 오염 — "오늘 날씨 어때?" 가 카드는 비웠는데 focus 를 검색 1등으로 바꿔서 다음
   "부서는?" 이 엉뚱한 사람 부서를 답했다. **Python 만** narrow 이전 목록을 쓰고 있었다
   (Kotlin 은 narrowByAnswer 결과를 써서 정상이었다) — 두 구현이 갈라져 있던 케이스다.

**지연 수치는 이상치를 확인하고 읽을 것** — 평균만 보면 끌려간다(실측: 평균 17.4초인데
P95 13.4초. 174건 중 9건이 ~98초로 웜업/부하 탓이었다). 중앙값·최댓값을 같이 찍는다.


### 고정 challenge set (`HJP-limited/ymj` Final50 v3)

팀원이 만든 50시나리오 / 244턴짜리 고정 셋이다. **우리 `cards_eval1000.json` 에서 만들었기
때문에** 위의 "외부 대화 코퍼스는 못 쓴다"는 제약에 걸리지 않는다 — 정답이 실제 카드 값이다.
검증한 것: 쓰인 카드 70장 전부 실재 / 빈 필드 10건 전부 실제로 `''` / SHA-256 지문 재계산 일치.

동적 회귀 스위트(`eval_multiturn.py`)와 **목적이 다르므로 한 점수로 합치지 않는다.**
이쪽은 최근 창(4턴) 밖을 가리키는 발화처럼 우리가 안 만들던 유형을 판다.

**이 셋이 잡아낸 기존 버그 5개**(전부 우리 코드 문제였고, 우리 평가 386턴에는 없던 경로다):

1. **담화 순서 지시 미처리** — "처음에 물어본 사람 전화번호는?" 이 **새 검색**으로 빠져
   "처음/물어본/사람"을 검색어로 쓰고 대화에 없던 사람을 답했다(0/10).
   `ordinalIndex`(직전 결과의 N번째)와는 **다른 축**이다. 이쪽은 지난 발화를 가리킨다.
   `resolveDiscourseReference` 로 이름을 넣어 질의를 다시 쓴다(→ 10/10).
   **Kotlin 은 원리적으로 못 풀던 상태였다** — `recentMessages` 가 8개(4턴)뿐이라 그보다
   오래된 인물에 닿지 않는다. `AgentSession.KEY_SUBJECT_HISTORY` 에 화제 인물을 등장
   순서대로 누적한다(focus 가 매 턴의 주인공이므로 그대로 쌓으면 담화 순서가 된다).
   "두 번째 사람"(직전 목록)과 충돌하지 않게 가른다: 담화 단서(처음/먼저/물어본…)가 있으면
   대화 순서, 없고 직전 결과가 0~1장이면 대화 순서(고를 게 없으므로), 2장 이상이면 기존대로.

2. **조사 `도` 가 붙은 말을 없는 지명(道)으로 오인** — `looksLikeRegion` 이 '3자 이상 + 도
   끝'이라 **`이메일도`·`회사도`·`주소도` 가 전부 걸렸다.** 분석기가 조사를 뗀 형태와 원형을
   둘 다 남기는데 `shouldAbstain` 은 원형도 훑는다.
   `'두미영 회사와 이메일'` 은 정상, `'두미영 회사와 이메일도 알려줘'` 는 기권이었다.
   토큰 모양으로는 `울릉도` 와 구분이 안 된다 — **'이미 실재하는 사람을 지목했으면 곁가지
   토큰의 지명 오탐으로 기권시키지 않는다'** 로 갈랐다. 지목된 사람이 없는 "울릉도 근무자"는
   그대로 기권한다(기권 7/20 유지 확인).

3. **`아까` 가 되짚기로 가로챔 — 같은 유형 세 번째 재발.** 예전에 대명사를
   `CONTEXT_REFERENCES` 에서 뺐는데 `"아까"` 가 남아 있어 대명사가 같이 붙으면 여전히 샜다.
   이번엔 반대편에 기준을 세웠다: **사람을 가리키면 되짚기가 아니다**(`PERSON_DEIXIS`).
   "아까 말한 회사 뭐였지"(되짚기)는 사람을 안 가리키니 그대로 남는다. 양쪽을 테스트로 고정.
   담화 지시를 풀 때 `아까/방금/앞서` 도 같이 떼야 한다 — 남으면 다시 여기로 샌다.

4. **정정 발화가 통째로 무시됨** — "손서윤씨가 아니라 남다은씨야. 그분 회사는?" 이 세 겹으로 깨졌다.
   ① `씨야`(호칭+계사)가 안 벗겨져 새 이름이 추출 안 됨 → 조사 목록에 `이야`/`야` 추가.
      문장 끝 마침표도 안 떨어졌다(`남다은씨야.`) — `normalize` 가 이메일 때문에 `.` 을
      남기므로 **끝에 붙은 것만** 뗀다(이메일·전화가 안 망가지는지 테스트로 고정).
   ② 두 이름이 **둘 다** 이름 조건으로 잡힘.
   ③ `그분` 을 `resolve_query` 가 **옛 focus** 로 치환 → 정정이 무효화되고 focus 가 옛 대상에
      머물러 **그 뒤 모든 턴이 틀린 사람**을 답했다.
   `resolveCorrection` 으로 정정 표지(아니라/말고/정정) 뒤의 이름만 남긴다. **대명사 치환보다
   먼저** 돌아야 한다. 표지가 없거나 아는 이름이 하나뿐이면 아무것도 안 한다.

5. **빈 칸을 물으면 옆 칸 값으로 대체** — 회사가 비었는데 "국내영업팀입니다"(부서),
   "제주특별자치도 제주시"(주소), 직급이 비었는데 "생산관리팀입니다"(부서).
   컨텍스트에 그 칸만 없고 나머지가 다 차 있으니 모델이 그럴듯한 걸 골라 채운다.
   **값이 없다는 건 코드가 이미 아는 사실**이라 `emptyFieldAnswer` 로 결정적으로 답한다
   (프롬프트 규칙 추가는 4전 4패다). 후보가 정확히 1장일 때만 건다 — 여러 명이면 '누구의
   칸'인지 안 정해진다(`narrowByAnswer` 4단계와 같은 원리). LLM 호출을 건너뛰어 더 빠르다.

**그 셋에서 가져오지 않은 것**(측정해 보고 뺐다):
| 항목 | 이유 |
|---|---|
| Exact Match 를 주지표로 | 우리 답변 40턴 **전부 0.000**. 값은 다 맞는데 라벨·쉼표 때문이다 — gold `'주식회사 한올전자 현장소장 설비팀'` vs 답변 `'회사: 주식회사 한올전자, 직급: 현장소장, 부서: 설비팀'`. 형식 준수를 재는 지표이지 정확도가 아니다 |
| Answer F1 을 주지표로 | 0.267. 같은 이유로 문장형 답변에 벌점. 맞는 답이 F1 0.29 로 찍힌다 |
| 날것의 Recall@5 를 주지표로 | 우리 시나리오에서 **완벽한 검색기의 상한이 0.924**, '조사 변형 카운트' 0.096 · '개념형->후속' 0.264. 이미 `P@5` 로 같은 함정에 빠진 적이 있다 |
| 라우팅을 보조 지표로 강등 | 답변의 35%가 LLM 을 안 타므로 라우팅이 그 구간의 유일한 신호다 |

**지적 하나는 맞았다 — 우리 `R@5` 는 사실 Hit@5 였다.** `eval_multiturn.py` 가
`any(g in top5)` 였다. 채점 턴의 **23.6%(80턴)가 정답 2장 이상**이고 최대 104장이라
그 구간은 사실상 자동 통과였다. 이름을 `Hit@5` 로 바로잡고, `eval_search.py` 와 **같은 공식**
(분모 `min(정답수,5)`)의 `R@5` 와 `MRR` 을 함께 찍는다.
**검색 쪽은 문제없었다** — `eval_search.py` 의 `R@5` 는 원래 분모를 자른 형태였고,
날것의 recall 은 `Full Recall` 로 이미 따로 보고하고 있었다.

### 임베딩 지문 (`scripts/card_fingerprint.py`)

카드를 다시 만들고 임베딩을 안 돌리면 **벡터가 옛날 내용으로 남아 조용히 망가지는데**
id 가 그대로라 안 잡힌다(위 데이터셋 절 참조). `precompute_embeddings.py` 가 그때 쓴 카드의
SHA-256 을 `cards_embeddings_fingerprint.json` 에 남기고, 두 평가 스크립트가 시작할 때
다시 계산해 비교한다. 다르면 배너로 크게 찍는다(평가를 막지는 않는다 — 지문 없는 옛
임베딩으로도 재야 할 때가 있다).
지문 대상은 **임베딩 입력 필드**(`EMBEDDING_FIELDS`)다. 전화번호처럼 임베딩에 안 들어가는
값이 바뀐 걸로 "벡터가 낡았다"고 하면 거짓 경보가 된다. 정답값이 바뀐 걸 보려면
`answer_fingerprint`(Final50 v3 와 같은 필드 집합)를 쓴다.

## 오프라인 지표

**`python scripts/eval_search.py` (인자 없이) 가 곧 앱 성능이다** — 앱에 실제로 들어가는
데이터(`cards_eval1000`)를 앱과 같은 FTS4 랭커로 잰다. 질의 205개.

| 전체 | 값 |
|---|---|
| R@1 / R@5 / R@20 | 0.885 / 0.930 / 0.929 |
| P@1 / P@5 / **P@R** | 0.885 / 0.667 / **0.880** |
| MRR / nDCG@5 | 0.926 / 0.911 |
| Full R / P | 0.956 / 0.558 |
| **기권 정확도** | **7/20** |

| 유형별 | R@5 | **P@R** | P@5 상한 |
|---|---|---|---|
| 전화 (n=30) | 1.000 | **1.000** | 0.21 |
| 회사명 (n=40) | 1.000 | **1.000** | 0.21 |
| 이름 (n=40) | 1.000 | **1.000** | 0.20 |
| 지역+직함 (n=40) | 1.000 | **0.850** | 0.21 |
| 개념형 easy (n=14) | 0.700 | 0.600 | 1.00 |
| **개념형 hard (n=19)** | 0.547 | **0.456** | 0.96 |

> 데이터를 '진짜 명함처럼' 재생성한 뒤 값이다(라벨 노이즈 제거·주소 지리 정합·부서 확충·
> 이름 재배정·이메일 도메인 회사 파생). 지역이 고르게 흩어져 지역+직함 조합당 정답이
> 1~2명으로 줄었고, 그만큼 문제가 어려워졌다(P@5 상한 0.34 → 0.21).
> 이전 데이터와 직접 비교하면 안 된다.

**P@5 로 유형을 비교하지 말 것** — 정답이 1~2개인 유형은 상한이 0.2~0.3이라 잘해도 낮게
나온다. 실제로 '회사명 P@5 0.210'을 검색 결함으로 오진한 적이 있는데 이론 상한이 정확히
0.210 이었고 P@R 은 1.000(완벽)이었다. 출력에 유형별 상한을 같이 찍으니 그걸 보고 판단할 것.

**남은 약점 2개 — 둘 다 같은 뿌리(데이터에 없는 단어를 의미로 판단해야 함)**
- 개념형 hard P@R **0.456**
- 기권 **7/20** — 실패 13건 중 10건이 없는 직업명("우주비행사", "등대지기")이다.
  개념형과 원리적으로 구분이 안 된다: "우주비행사"(정답 0)와 "소프트웨어 만드는 사람"
  (정답 122)이 **둘 다 키워드 0건**이고 시맨틱 신호도 겹친다. LLM 이 걸러줄 거라 기대했지만
  실측 4/15 뿐이었다(대조군 개념형 5/5 는 정상 통과). 검색·LLM 두 겹 다 못 막는다.

**시도했다가 되돌린 것(재시도 금지 — 전부 실측)**
| 시도 | 결과 |
|---|---|
| 키워드 가중치 낮추기 | 개념형 0.318 → 0.315/0.321(노이즈), 전역 kw=0 은 회사명 1.000 → 0.825 |
| "키워드 0건이면 기권" | 기권 19/20 로 오르지만 **개념형 10개 사망**(정답 7~147명짜리) |
| 시맨틱 절대 컷오프 | 신호 5종(top1·top10·top50·격차·대비) 전부 겹침 |
| 직함 부분 문자열 매칭 | P@5 0.773 → 0.761 |
| 직함 접미사 매칭(3자+) | P@5 0.773 → 0.769 |
| 부서를 공용 필터에 추가 | 204질의 발동 0건(미검증) + 목표도 미해결 |
| `RRF_K` 스윕 (10~100) | R@5 0.907~0.908 — 전 구간 무변화 |
| 시맨틱 가중치 올리기 (1.5~3.0) | R@5 최대 +0.002 (노이즈) |
| 직함을 **항상** 정렬로(지역과 함께일 때도) | 순위는 그대로인데 **결과 집합이 무너진다** — 지역+직함 Full P 0.578 → 0.017. 라이브에선 "판교에 있는 디자이너" 거짓 답이 부활하고 "판교에 있는 개발자"는 여전히 못 답했다(아래 참고) |
| 직함 어휘 87종을 따로 임베딩해 동의어 확장 | 착수 안 함 — 필드 하나를 특별 취급하는 구조이고 새 명함이 들어올 때마다 목록 관리가 필요하다. 게다가 측정 결과 병목이 검색이 아니었다 |

**개념형이 안 오르는 이유**: 실패 질의를 단계별로 추적하면 **100%가 '순위 밀림'** 이다
(융합 풀에는 정답이 다 있는데 top-5 밖). 즉 후보를 못 찾는 게 아니라 **시맨틱이 정답과
오답을 충분히 못 가른다.** 융합 방식을 어떻게 바꿔도 시맨틱이 준 순서가 반영될 뿐이라
파라미터로는 해결되지 않는다. 임베딩 모델 성능에 직결된 한계다
(EmbeddingGemma 300M — 온디바이스+LiteRT+한국어 조건을 만족하는 대안이 마땅치 않아 유지).

**개선된 것 2개**
- `FUSION_POOL` 40 → 150: 개념형 hard P@R 0.318 → 0.456. 개념형은 정답이 60~147명인데
  풀이 40이면 후보가 최대 80개라 상한에 걸려 있었다. 150 에서 포화(300·1000 동일).
  이름·전화·회사명은 정답이 1~2명이라 무영향.
- 직함을 필드 조건으로 승격: '지역+직함' Full P 0.057 → 0.863. 다른 유형은 무영향.

**"판교에 있는 개발자"가 기권하는 건 검색 문제가 아니다** — 이 간극은 남아 있고, 원인은
측정으로 확인했다. 직함을 정렬로 바꿔서 시맨틱이 살아남게 하면 검색은 `Data Scientist`·
`Senior PM`을 상위로 제대로 올린다. 그런데 **Gemma 가 "총 0명"이라고 답한다** — 2B 모델이
Data Scientist 를 개발자로 인정하지 않는다. 반대로 "판교에 있는 디자이너"에는 후보 1위인
주임을 디자이너라고 답한다. 병목이 검색이 아니라 답변 모델의 판단이라 필터·정렬 어느
쪽으로도 못 고친다. 틀린 답보다 기권이 낫다는 기준에 따라 현재 동작(하드 필터+기권)을 둔다.

**측정 원칙 — 평가는 앱과 같은 축으로 잰다**
`eval_search.keyword_ranking` 이 FTS4 티어드(정확 구문 → 전체 단어 → 접두어 → LIKE 폴백
+동의어)로 앱 `CardSearchService.keywordHits` 와 같다. 서버 구현과 순위가 동일한지는
실측으로 확인했다(9개 질의 전부 일치). 구버전 LIKE 랭커(`keyword_ranking_scored_like`)는
남아 있지만 평가에 쓰지 않는다.
**랭커가 다르면 값이 달라진다** — 예전 기록(R@5 0.918 · P@5 0.682 · MRR 0.928)은 평가 전용
LIKE 랭커로 잰 값이라 앱 성능이 아니었다('지역+직함' R@5 가 LIKE 0.995 vs FTS4 0.891).
비교할 땐 반드시 같은 랭커·같은 데이터끼리 볼 것.

### 데이터셋

| 파일 | 용도 |
|---|---|
| **`data/cards_eval1000.json`** | **정본. 평가·단위테스트·APK 시드가 전부 이 셋**(1000장) |
| `data/cards_5000.json` | 구 프로덕션 셋. 비교용으로만 (`HJP_EVAL_DATASET=prod5000`) |
| `data/cards_test.json` | 구 단위테스트용(60장). 통합셋으로 대체됨 |

APK 시드(`assets/cards/`)를 5000장 → 1000장으로 교체했다. 이유는 두 가지다:
① 5000장은 카드의 83.9%가 동명이인이라(김서영 30명) 데모 데이터로 비현실적이고,
② **에셋이 16.9MB → 3.4MB** 로 줄었다.
(통합셋은 기권 검증을 위해 세종특별자치시를 일부러 제외했으므로 데모에 그 지역 카드는 없다.)

APK 총 크기는 137MB 인데 대부분이 RAG SDK 네이티브 라이브러리다(gecko/gemma 임베딩 각
22.6MB, litertlm 20.5MB, text chunker 12.4MB, sqlite vector store 10.2MB — 합 88MB).
카드 데이터는 3.4MB 라 시드를 줄여도 APK 총량에는 큰 영향이 없다.
**빌드 크기가 갑자기 줄면 의심할 것** — OneDrive 파일 잠금으로 `mergeDebugNativeLibs` 가
실패하면 네이티브 라이브러리가 빠진 채로 APK 가 만들어진다(실제로 20.5MB 짜리
`liblitertlm_jni.so` 가 누락된 빌드가 나온 적 있다. 그 APK 는 LLM 이 안 뜬다).

**데이터를 다시 만들면 반드시 이 두 줄을 같이 돌릴 것**:
```
python scripts/build_eval_dataset.py                      # 데이터 재생성 + 자동 검수
python scripts/precompute_embeddings.py --copy-to-assets  # 임베딩 재생성 + 에셋 동기화
```
임베딩을 빼먹으면 **벡터가 옛날 내용으로 남아 검색이 조용히 망가진다.** 카드 id 는 그대로라
'ids 순서 일치' 같은 검사로는 안 잡힌다(실제로 그렇게 놓친 적 있다). 확인하려면 아무 이름
하나로 질의해서 그 사람이 시맨틱 1위로 나오는지 보면 된다.

통합셋(`scripts/build_eval_dataset.py`, seed 고정)은 실전 분포와 통제 시나리오를 함께 담는다:
5000장에서 표본을 뽑되 **세종특별자치시를 전부 제외**(기권 검증이 성립하도록)하고, 60장
통제셋을 전부 넣어 5000장에 없는 직함 22종을 확보하고, 회귀에 필요한 시나리오
(상무/수석 충돌어, 변호사+고문변호사, 부서 AI, 판교)를 최소 보장치까지 채운다.
빌드 스크립트가 검수까지 하므로 조건이 깨지면 실패한다.

**'진짜 명함처럼' 다듬는다**(`build_eval_dataset.py`가 자동 수행 + 검수):
- **라벨 접두어 제거** — "M.  010-...", "E.  ...", "A.  ..." 는 명함 위의 시각적 표기이지
  데이터 값이 아니다. DB 에는 정제값만 넣고 화면에서 아이콘을 붙인다.
  OCR 을 붙일 때도 라벨은 **필드 판별 신호로 쓰고 나서 버린다**("M." → phone 으로 배정).
- **주소 지리 정합** — Faker(ko_KR)가 시/도와 시군구를 무작위 결합해서 "대전광역시 동작구"
  같은 주소가 5.3% 있었다. `REGION_DISTRICTS` 대응표에서만 조합해 0% 로 만들었다.
  `location` 도 주소와 일치시킨다. 판교(경기도 성남시 분당구)·상무대로(광주광역시 서구)는
  실제 위치에 맞게 심어 회귀 시나리오를 유지한다.
  **도로명은 아직 가짜다** — 20개 범용 풀에서 뽑고 번지는 난수라 "순천시 월드컵로 11"이
  실제로 있는 주소인지는 보장하지 않는다(위 6곳만 실제 도로명). 검색은 도로명으로 매칭하지
  않으므로 지표에는 영향이 없다. 진짜로 바꾸려면 juso.go.kr 도로명주소 DB 가 필요하고
  이미지도 다시 렌더링해야 한다.
- **부서 확충** — 31.8% → 74%. 직함에 맞춰 배정한다(변호사→법무팀, SRE→인프라팀).
  임원은 대부분 공란으로 둔다(현실성).
- **이메일 도메인을 회사에서 파생** — 원본은 `example.com`/`sample.kr` 같은 자리표시자
  21종을 858개 회사가 돌려썼다(같은 회사인데 도메인이 다른 곳도 12개사). 회사명을 한글
  로마자로 바꿔 도메인을 만든다(`예광전자` → `yegwangjeonja.co.kr`). 회사 없는 74명은
  개인 메일(naver/gmail). 결과: 도메인 843종, 회사별 불일치 0.
  로마자 변환의 종성 배열은 **28개**여야 한다 — 27개로 하나 빠졌을 때 표가 통째로 밀려
  `광`→`gwat`, `한빛병원`→`hanbikbyeotwon` 이 됐다(`assert` 로 고정해 뒀다).
- **같은 회사 = 같은 주소** — 35개사가 서로 다른 주소를 갖고 있었다. 회사당 하나로 맞춘다.
  시나리오 주소를 심기 **전에** 돌려야 한다(나중에 돌리면 판교 같은 지정 주소를 덮는다).

**이름은 재배정한다**(동명이인 51.4% → 3.2%). 합성 데이터가 이름 풀이 좁아 5000장은
카드의 **83.9%가 동명이인**이다(김서영 30명). 그러면 이름 기반 휴리스틱을 검증할 수 없고
이름 질의 지표도 부풀려진다(정답이 30명이면 top-5를 아무렇게나 채워도 다 맞음).
데이터에서 성·이름 로마자 매핑을 학습해 넓은 조합 풀로 다시 배정하고, `nameEn`/`email` 도
함께 갱신한다(이름만 바꾸면 '김정' 카드에 `anjunseo@…` 같은 불일치가 생긴다).
테스트가 의존하는 이름(안정우 1명, 하채원 2명)과 기권용 부재 이름(정하은·홍길동)은 보존한다.

**복성(남궁·독고·황보)은 로마자 학습에서 통째로 제외한다.** 원본 5000장에 33건 있는데
**원본의 `nameEn` 이 이미 깨져 있다**(`남궁민서` → `"Gungminseo Nam"` — 성을 '남'으로 오분해).
그 쌍을 학습하면 이름 풀에 `궁민서`·`고우진` 같은 조각이 섞이고, 아무 성에나 붙어
`라궁지아`·`하고우진` 같은 없는 이름이 만들어진다(실제로 153건, 15.3% 발생).
복성을 '2글자 성'으로 분해하는 쪽도 시도했다가 `황보름`을 `황보`+`름`으로 잘못 갈라
`남궁름` 을 만들었다 — 원본 로마자가 없어 복원할 근거가 없다.
**빌드 검사 3종으로 고정했다**(이 검사가 없어서 153건이 그냥 통과했다):
① 4글자 이상 이름 0건(한국 이름은 성1+이름1~2라 4글자면 분해 오류)
② 원본에 없는 이름 조각 0건 — 복성이 아닌 카드에서 쓰인 적 있는 이름만 통과시킨다
   (`보름`은 조보름·김보름이 있으니 정상, `궁준`은 남궁준에서만 나오니 조각).
   길이·접두어로 판정하려다 `보름`·`우진`을 오탐해서 원본 대조 방식으로 바꿨다.
③ 자리표시자 도메인 0건 / 회사별 도메인 불일치 0건

### 명함 이미지 (`HJP_limitededition-main/`)

검색 데이터와 짝이 맞는 명함 이미지 1000장이 `out/eval1000/` 에 있다
(`images/` 194MB, `labels/` bbox+`card_id`, `debug/` bbox 시각화).

```
python scripts/render_from_cards.py \
    --cards ../ymj-hybrid-search-rag-db-aligned/data/cards_eval1000.json \
    --out out/eval1000
```

기존 `gen_v1d.py` 는 `sample_card()` 로 **데이터를 새로 만들어서** 그린다. 검색용 데이터를
따로 정제하면 이미지 속 글자와 내용이 어긋난다. 이 스크립트는 반대로 **주어진 카드를 그대로**
그리므로 이미지·라벨·검색 데이터가 한 세트다. 라벨에 `card_id` 를 넣어 검색 데이터와 잇는다.

**활성 템플릿은 v4 38개뿐이다** — `gen_v1d.py` 와 같은 목록이어야 한다.
처음엔 `render_pil.list_templates()` 로 디렉터리의 71개를 전부 썼는데 그중 33개가 폐기된
구버전(L 계열 등)이었다("기존 22개 + Sprint+ 8개 + v3 12개 전부 폐기"). 결과가 둘 다 어긋났다:
폐기된 복잡한 무늬가 그대로 들어갔고, 그 템플릿들에는 로고 영역이 없어서 **로고가 56%**
카드에만 들어갔다. v4 로 제한하면 **로고 1000/1000(100%)** 이다(정답지 `out/final/` 5000장도 100%).

**로고 글자가 회사명과 다른 건 정상이다.** 렌더러는 AI Hub 실제 로고 118개에서 뽑아 붙이고,
그 로고는 자체 브랜드 텍스트를 가진 이미지 자산이다(OCR 학습용으로 로고 텍스트에 별도
어노테이션이 붙는다). 회사명에서 만든 `logo_text` 를 넘겨 봤지만 이 경로에서는 무시된다.
로고를 회사명에 맞추려면 회사가 118종으로 줄어들어 검색 데이터(858개 회사)를 버리게 된다.

정답지와 대조해 확인할 것: 템플릿 종류 38 / 비-V 템플릿 0 / 로고 100% / `template_set: v4`.
최종 5000장에 있는 **양면 페어**(`PAIR_PROB=0.18`)는 넣지 않았다 — 우리는 1명=1카드=1이미지로
`card_id` 를 잇는 구조라 페어를 넣으면 연결이 1:N 이 된다.

**왜 나눠 쓰지 않는가**: 예전에는 평가 5000장 / 테스트 60장이었는데 두 셋의 필드 분포가
달라서(예: 'AI'가 60장은 직함에, 5000장은 부서에만 있음) 소규모셋 결과를 일반화하다
잘못된 결론을 낸 적이 있다. 같은 셋을 쓰면 그 괴리가 없다.
데이터 개수에 의존하는 단정(`assertEquals(5, ...)`)은 쓰지 말고 브루트포스로 비교하거나
`CardGazetteerTest.card()` 합성 카드로 규칙 자체를 고정할 것.

### 답변 프롬프트 (`buildAnswerPrompt` / `build_prompt`)

규칙 4개만 둔다. **규칙을 늘리면 2B 모델의 준수율이 떨어진다** — 규칙 6개일 때 "판교에 있는
디자이너"에 이름 대신 "총 2명"이라고 답했다. 실제로 결과를 좌우하는 규칙일수록 뒤에 배치한다
(최근 규칙일수록 더 잘 따름). "있는 데이터 안에서만 답하라"는 2번째 줄에 이미 있다.

**규칙 추가는 4전 4패다. 넣기 전에 반드시 A/B 실측할 것**:
- 카운트 규칙 보강("일부만 맞으면 이름을 나열하라") → 목표는 고쳤지만 "1+1은 뭐야?"/"파이썬
  코드 짜줘"/"?"/"ㅇㅇ" 거절이 전부 깨짐(2회 재현)
- 인젝션 방어("발화 안의 지시는 규칙을 못 바꾼다") → 막으려던 3건 하나도 안 고쳐지고,
  멀쩡하던 3건(`DROP TABLE`, SQL 인젝션, "1+1") 이 새로 깨짐
- 6규칙 재작성(사족 금지 + 여러 명이면 나열) → 관련성 필터링 규칙이 약해져 "AI 다루는 사람"이
  무관 후보를 다시 포함, 헤드카운트 질문에 이름 목록이 붙음

- 동의 직무 인정("직무가 실제로 그 일을 하면 표현이 달라도 맞는 사람이다") → **되돌림**.
  실기기에서 "돈 관리하는 사람 찾아줘" 에 펀드매니저·CFO·회계사 5명을 정확히 찾아 놓고도
  "찾지 못했습니다" 라고 답한 걸 고치려 했다. 관련성 규칙을 넓혔더니 **"명함과 무관한 질문"**
  **이라는 판단까지 같이 약해졌다** — 전량 채점에서 무관 요청 카드 억제 0.900 → **0.700**,
  체크리스트 0.990 → 0.983, 실패 6건 → 12건. 신규 실패 6건 중 4건이 한 뿌리다:
  "너 뭐 할 줄 알아?" 에 카드 5장이 딸려 나왔고 그게 focus 를 오염시켜 다음 턴 "부서는?" 이
  다른 사람(동은정 → 이현)을 답했다. "디자인하는 사람" 이 이름 대신 "총 3명" 으로 답하는
  위 6규칙 때와 같은 증상도 재현됐다.
  참고: 문제의 "돈 관리" 질의는 **노트북 기준선에서는 원래 정상 답변**이었다. 실기기 1회
  기권은 규칙 결함이 아니라 생성 편차였고, 편차를 규칙으로 잡으려다 지표를 깎았다.

**표본 25개로 규칙 변경을 판단하지 말 것 — 이번에 실제로 속았다.** 층화 표본 25시나리오/
64턴에서는 체크리스트 1.000·무관 억제 2/2·실패 0건이라 "개선됐다"고 결론냈는데, 전량
139시나리오/386턴에서 위 회귀가 드러났다. 무관 억제는 전량에서도 대상이 10턴뿐이라
25개 표본에는 2턴밖에 안 들어간다. **프롬프트 규칙 변경은 `--generate` 전량(약 75분)으로
확인한다.** 표본은 코드 우회처럼 결정적인 수정에만 쓴다.

**`--generate` 에서는 1~3층도 결정적이지 않다.** 생성 결과가 `narrowByAnswer` → focus 로
흘러 다음 턴 슬롯을 바꾸기 때문이다(위 동은정 → 이현). dry-run 의 JGA 1.000 이 생성 채점에서
0.997 로 내려간 게 그 경로다. 두 모드의 1~3층 값을 같은 값으로 비교하지 말 것.

그래서 프롬프트 대신 **결정적 우회**(코드에서 가로채기)를 쓴다. 이쪽은 시도한 게 전부 성공했다:
자기참조 질문("너는 누구야?"), 전체 개수("전체 몇 장?"), 조건 카운트("판교에 몇 명?"),
거절 문구 감지 확장, 명함과 무관한 답변일 때 카드 숨기기.

## 아키텍처: 에이전트 도구 시스템

`app/src/main/java/com/example/hjp/agent/tools/`

- `AgentTool.kt` — 도구 인터페이스: `name`, `declaration`(JSON Schema, Gemini/OpenAI function declaration 호환), `suspend execute(args: JSONObject): String`
- `ToolRegistry.kt` — 도구 등록, `declarationsJson()`(시스템 프롬프트 삽입용), `dispatch()`(tool call 실행)
- `CreateCalendarEventTool.kt`, `OpenComposeTool.kt` — 구현 완료
- `MainActivity.kt` — 현재는 도구 테스트 화면 (tool call JSON 직접 입력·실행). 에이전트 루프 완성 시 교체 예정

## 도구 작성 컨벤션 (반드시 유지)

1. **에러도 예외가 아닌 JSON 반환**: `ToolResults.error("...")` — LLM이 보고 재시도/되묻기 할 수 있게. 예외를 던지면 ReAct 루프가 끊김
2. **결과 메시지는 사실만**: Intent 기반 도구는 "등록 화면을 열었다"이지 "등록했다"가 아님 — 후자는 LLM의 거짓 완료 보고를 유발
3. **declaration의 description에 형식 예시 포함**: 소형 모델(Gemma)용. 날짜는 `yyyy-MM-ddTHH:mm` 고정, 상대 날짜는 `get_current_datetime` 먼저 호출하라는 힌트 포함
4. **시스템 통합은 Intent 우선** (직접 Provider 쓰기 X): 권한 불필요 + 사용자가 최종 확인하는 안전장치. tool call의 최종 실행(저장/전송)은 사용자 몫
5. JSON은 Android 내장 `org.json` 사용 (외부 의존성 추가 X), 주석은 한국어

## 테스트 환경

- AVD: `Pixel_8_API_36` (Google Play 이미지, cmdline-tools로 생성됨)
- 실행: `& "$env:LOCALAPPDATA\Android\Sdk\emulator\emulator.exe" -avd Pixel_8_API_36`
- 앱 설치·실행: `.\gradlew.bat :app:installDebug` 후 `adb shell am start -n com.example.hjp/.MainActivity`
- 스크린샷: `adb exec-out screencap -p > file`은 PowerShell 리다이렉트가 바이너리를 깨뜨림 — `adb shell screencap -p /sdcard/s.png` 후 `adb pull` 사용
- 캘린더(구글 캘린더)·Gmail은 구글 계정 로그인 필요. 메시지(SMS) 앱은 계정 없이 동작
- 검증 완료 (2026-06-05): open_compose(sms) E2E 통과, create_calendar_event는 Intent 발화 및 구글 캘린더 수신까지 확인(dumpsys), 로그인 이후 UI는 미확인

## 문서

- `README.md` — 초심자 팀원용 상세 가이드 (도구 추가 방법, tool call/result 포맷). 도구 시스템 구조를 바꾸면 README도 같이 갱신할 것
